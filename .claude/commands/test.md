프로젝트 테스트를 실행합니다.

1. `.venv` 가상환경을 활성화한 뒤 `pytest -v` 실행
2. 테스트 결과 요약 보고 (passed/failed/error 수)
3. 실패한 테스트가 있으면 원인을 간략히 분석
4. 샌드박스 환경에서 pytest가 프로젝트 루트에 임시 디렉토리(`pytest-of-*`)를 생성할 수 있음 — 테스트 완료 후 해당 디렉토리가 존재하면 삭제하여 정리
